@* <auto-generated> - Template:AdminEditPage, Version:2021.12.3, Id:bf6a5c3b-7d19-4f08-83e6-c341bc350f81 *@
@page "/admin/artworkcategoryxrefedit/{ArtworkId:int}/{CategoryId:int}"
@inherits ASArtworkCategoryXrefEditViewModel
<div class="mud-palette-override"> @* This outer div is necessary for CSS Isolation to function *@


    @if (!IsReady)
    {
        <MudText>
            Loading....
        </MudText>
    }

	@if (!Saved)
	{
			<section class="artworkcategoryxref">
			<MudText Typo="Typo.h6" GutterBottom="true">@ArtworkCategoryXref?.ArtworkId</MudText> @* Replace with an appropriate identifying string property *@
			<EditForm Model="@ArtworkCategoryXref" OnValidSubmit="OnValidSubmit">
				<DataAnnotationsValidator />
				<MudCard>
					<MudCardContent>
						<label for="">Your Primary Key Here</label>
						@* Add your editor controls for editing the object here, within the MudCardContent *@
					</MudCardContent>
					<MudCardActions>
						<MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Color="Color.Primary" Class="ml-auto">Save</MudButton>
						<a class="btn btn-secondary m-2" @onclick="@ReturnToList">
							Cancel
						</a>
					</MudCardActions>
				</MudCard>
				<MudText Typo="Typo.body2" Align="Align.Center" Class="my-4">
					Fill out the form correctly to see the success message.
				</MudText>

                <MudExpansionPanels>
                    <MudExpansionPanel Text="Show Validation Summary">
                        @if (Saved)
                        {
                            <MudText Color="Color.Success">Success</MudText>
                        }
                        else
                        {
                            <MudText Color="@Color.Error">
                                <ValidationSummary />
                            </MudText>
                        }
                    </MudExpansionPanel>
                </MudExpansionPanels>
			</EditForm>
			</section>
	}

	else
    {
        <div>
            <a class="btn btn-secondary m-2" @onclick="@ReturnToList">
                Return to list
            </a>
        </div>
    }
@if (!string.IsNullOrWhiteSpace(Message))
    {
        <div class="alert @StatusClass">
            @Message
                <MudButton @onclick="@ClearMessage" Variant="Variant.Filled" Color="Color.Error" Style="height: 12px; width: 12px; min-width: 12px; padding: 1px; top: -6px;">
                    <i class="fas fa-times"></i>
                </MudButton>
        </div>
    }</div>
