@typeparam TItem

@if (WaitFor == null)
{
    <MudProgressCircular Color="MudBlazor.Color.Primary" Indeterminate="true" />
}
else
{
    @ChildContent
}

@code {
    [Parameter]
    public TItem WaitFor { get; set; }

    [Parameter]
    public RenderFragment ChildContent { get; set; }
}
